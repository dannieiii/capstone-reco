# Copy this file to .env or .env.local and set your OpenWeatherMap API key
# Vue CLI uses VUE_APP_ prefix for env vars available in client code
VUE_APP_WEATHER_API_KEY=

# Alternative (Vite) if you migrate in the future
VITE_WEATHER_API_KEY=

# Optional runtime override without rebuild (will be read from window/localStorage)
# window.OWM_API_KEY can be set in index.html for quick testing
# localStorage.setItem('WEATHER_API_KEY', '<your-key>')
